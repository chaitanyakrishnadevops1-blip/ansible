- name: Install packages
  hosts: local
  connection: local
  bacome: yes
  tasks:
    - name: Install required packages
      ansible.builtin.package:
        name: "{{ item }}"
        state: latest
      loop:
        - nginx
        - mysql
        - zip
