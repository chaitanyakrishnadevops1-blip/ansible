- name: Install packages
  hosts: local
  connection: local
  bacome: yes
  tasks:
    - name: print names
      ansible.builtin.package:
        name: "{{ item }}"
        state: latest
      loop:
        - nginx
        - mysql
        - zip
